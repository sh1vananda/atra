
"use client";

// This file is no longer used as its functionality has been merged into /src/components/layout/Header.tsx
// It can be safely deleted.

// To prevent build errors if it's somehow still imported, leave a minimal export:
export function AdminHeader() {
  return null; 
}
